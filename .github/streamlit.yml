name: CI Quality Check

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Set up Python
        uses: actions/setup-python@v3
        with:
          python-version: "3.11"
          
      - name: Install dependencies
        run: |
          pip install -r agent/requirements.txt
          
      - name: Run Structural Tree Validation
        run: |
          python tree/validate_tree.py
